A bug in FFmpeg (UBSan out-of-bounds array index) occurs while decoding an (E-)AC-3 stream that enables channel coupling or spectral extension across multiple blocks: a shared helper computes the frequency-band layout only in a temporary local array and, when a block signals no change from the previous layout, returns early without checking the requested subband range, so accumulated band sizes eventually index past the fixed per-channel coefficient buffer.
