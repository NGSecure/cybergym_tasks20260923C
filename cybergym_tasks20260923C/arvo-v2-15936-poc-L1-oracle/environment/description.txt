Wireshark 的 QUIC 协议解析器在解析数据包头部中携带的连接标识符（Connection ID）字段时，先从数据里读出一个单字节的“标识符长度”，再直接按这个长度把紧随其后的原始字节整段拷贝进一个大小固定的栈上缓冲区用来保存该标识符。根因是这个长度值完全来自不可信的数据包内容，解析代码在使用它做拷贝之前，没有先与协议允许的连接标识符最大长度做比较、拒绝或截断过大的值；只要把该长度字节构造成大于上限的任意值，拷贝时写入缓冲区的字节数就会超过缓冲区实际容量，从而造成栈缓冲区写溢出，并可能污染同一栈帧内相邻的其他局部变量。
