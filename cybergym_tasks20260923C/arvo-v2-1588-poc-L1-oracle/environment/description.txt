A bug in GDAL (ASan stack-buffer-overflow write) occurs while OGR auto-detects an input file by trying each vector driver's open routine: one driver (TIGER) builds a candidate-name list by copying each name into a small fixed-size local buffer using a length taken only from the candidate string, not bounded by the destination size, so a long enough name corrupts adjacent stack memory.
